types:
  GameStatus:
    - SEATING
    - STARTED
    - FINISHED
  Player:
    id: UserId
    color: Color
  State:
    status: GameStatus
    players: Player[]
    activePlayer: UserId?
    moves: string[]
  Color:
    - RED
    - GREEN
    - BLUE
    - WHITE

methods:
  createGame:
  joinGame:
    color: Color
  startGame:
  endGame:
  makeMove:
    command: string

auth:
  anonymous: {}
  nickname: {}

userState: State
error: string
